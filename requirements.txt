# Core Apify SDK (Python) v1.x
apify>=1.7.2,<2.0.0

# HTTP client for webhooks/Slack notifications
httpx>=0.27.0,<0.28.0

# Optional: faster JSON handling (used if available; standard json is fallback)
orjson>=3.10.0,<4.0.0
